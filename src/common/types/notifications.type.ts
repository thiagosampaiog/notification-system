export enum NotificationChannel {
  EMAIL = 'Email',
  SMS = 'SMS',
  PUSH = 'Push'
}

export enum NotificationPriority {
  HIGH = 'High',
  MEDIUM = 'Medium',
  LOW = 'Low'
}

export enum NotificationStatus {
  PENDING = 'pending',
  SENT = 'sent',
  FAILED = 'failed'
}
